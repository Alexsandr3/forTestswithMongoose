export type BodyParams_newPasswordInputModel ={
    /**
     * New account recovery password.
     */
    newPassword: string
    /**
     * Code that be sent via Email inside link
     */
    recoveryCode: string
}
