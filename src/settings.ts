export const settings = {
    ACCESS_TOKEN_SECRET: process.env.PORT || "secret key Belarus",
    REFRESH_TOKEN_SECRET: process.env.PORT || "secret key jive",
    ACCESS_TOKEN_SECRET_ADMIN: process.env.PORT || "superman from moon",
    REFRESH_TOKEN_SECRET_ADMIN: process.env.PORT || "superman"
}